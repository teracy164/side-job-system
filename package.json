{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "postinstall": "lerna bootstrap",
    "build": "lerna run build:prod",
    "start": "lerna run start:prod",
    "dev": "docker-compose --file=docker-compose.yml --file=docker-compose.dev.yml up"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
